---
description: Validate a saved Build context and resume the recorded Build strategy.
---

Use the AbsolutForge `load` skill.

Arguments: the canonical `save-{slug}.md` path.

Supplied arguments: $ARGUMENTS

Read the `load` skill and follow its contract exactly, including every precondition, artifact schema, and safety boundary it links. If a required argument is missing, ask for it instead of guessing.

Do not restore source, switch strategy, or write repository changes.
