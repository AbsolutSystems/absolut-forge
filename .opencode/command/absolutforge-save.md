---
description: Persist concise resume context for an active Building feature.
---

Use the AbsolutForge `save` skill.

Arguments: the canonical Feature Brief path.

Supplied arguments: $ARGUMENTS

Read the `save` skill and follow its contract exactly, including every precondition, artifact schema, and safety boundary it links. If a required argument is missing, ask for it instead of guessing.

Write only the save artifact. Do not commit, stash, or change branches.
