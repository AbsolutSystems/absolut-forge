---
description: Implement an accepted Ready Feature Brief via planned test-driven tasks.
---

Use the AbsolutForge `build-planned-tdd` skill.

Arguments: the canonical Feature Brief path.

Supplied arguments: $ARGUMENTS

Read the `build-planned-tdd` skill and follow its contract exactly, including every precondition, artifact schema, TDD cycle, and safety boundary it links. If a required argument is missing, ask for it instead of guessing.

This selects the `planned` Build strategy with `tdd` methodology for the feature and records both in Build start evidence.
