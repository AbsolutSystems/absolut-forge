---
description: Close a review-complete feature into durable records and one local commit.
---

Use the AbsolutForge `ship` skill.

Arguments: the Feature Brief path, then the review artifact path.

Supplied arguments: $ARGUMENTS

Read the `ship` skill and follow its contract exactly, including every precondition, artifact schema, and safety boundary it links. If a required argument is missing, ask for it instead of guessing.

Present one approval preview first. Never push, open a PR, merge, deploy, or rewrite history.
