---
description: Implement an accepted Ready Feature Brief through a durable planned task graph.
---

Use the AbsolutForge `build-planned` skill.

Arguments: the canonical Feature Brief path.

Supplied arguments: $ARGUMENTS

Read the `build-planned` skill and follow its contract exactly, including every precondition, artifact schema, and safety boundary it links. If a required argument is missing, ask for it instead of guessing.

This selects the `planned` Build strategy and `standard` methodology for a new feature. A recorded legacy delegated feature also resumes through this command; preserve its fixed-executor ownership and profile rather than converting it to standard work.
