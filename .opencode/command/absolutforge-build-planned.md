---
description: Implement an accepted Ready Feature Brief via a planned task graph with delegated workers.
---

Use the AbsolutForge `build-planned` skill.

Arguments: the canonical Feature Brief path.

Supplied arguments: $ARGUMENTS

Read the `build-planned` skill and follow its contract exactly, including every precondition, artifact schema, and safety boundary it links. If a required argument is missing, ask for it instead of guessing.

This selects the `planned` Build strategy for the feature and records it in Build start evidence.
