---
description: Plan an accepted Ready Feature Brief for a fixed executor that owns every implementation edit.
---

Use the AbsolutForge `build-planned-delegated` skill.

Arguments: the canonical Feature Brief path.

Supplied arguments: $ARGUMENTS

Read the `build-planned-delegated` skill and follow its contract exactly, including every precondition, artifact schema, fixed-executor requirement, and safety boundary it links. If a required argument is missing, ask for it instead of guessing.

This selects the `planned` Build strategy with `delegated` methodology for the feature and records both in Build start evidence.
