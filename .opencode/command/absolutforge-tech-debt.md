---
description: Run a bounded read-only technical-debt audit.
---

Use the AbsolutForge `tech-debt` skill.

Arguments: an optional repository-relative path to bound the audit.

Supplied arguments: $ARGUMENTS

Read the `tech-debt` skill and follow its contract exactly, including every precondition, artifact schema, and safety boundary it links. If a required argument is missing, ask for it instead of guessing.

Read-only. Produce a prioritized, evidence-backed backlog; change no files.
