interface:
  display_name: "AbsolutForge Save"
  short_description: "Persist concise Build resume context"
  default_prompt: "Use $absolutforge save to capture the current Building feature state before I switch branches."
