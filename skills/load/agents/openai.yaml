interface:
  display_name: "Load Progress"
  short_description: "Restore saved Build context for a paused feature"
  default_prompt: "Use $load to restore this saved AbsolutForge Build context."

policy:
  allow_implicit_invocation: false
