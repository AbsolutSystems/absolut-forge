---
name: build
description: "Explicitly implement an accepted Ready Feature Brief autonomously, with verified checkpoints and a whole-feature Review handoff. Use only when the user invokes AbsolutForge build."
---

# Build — Autonomous Strategy

Use this entrypoint only with the canonical Feature Brief path. Read [runtime common](../../runtime/common.md), then the [autonomous runtime](../../runtime/autonomous.md), and the matching active-host mechanics in `../../references/`. Those documents own the execution loop, start/resume checks, evidence, final verification, and handoff.

`Ready` starts autonomous Build. A `Building` Brief resumes here only when its recorded strategy is autonomous; route planned state to `build-planned`. Do not switch recorded strategy or methodology.

At an initial Ready start, read verification doctrine as required by the autonomous runtime; also read the relevant artifact-contract sections at Build start and final delivery-gate verification. Follow the runtime's full remaining triggers, including doctrine for uncertain obligations or exemptions and the harness contract when producing the final Review continuation. The runtime specifies the required resolved Brief/Review paths and copy-ready final handoff.
