interface:
  display_name: "Ship"
  short_description: "Close a reviewed AbsolutForge feature into a local archive and commit"
  default_prompt: "Use $ship with the final Brief and Review paths: absolutforge/features/{slug}/feature-brief.md absolutforge/features/{slug}/review.md"

policy:
  allow_implicit_invocation: false
