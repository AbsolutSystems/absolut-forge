interface:
  display_name: "Debug"
  short_description: "Diagnose and fix a concrete failure"
  default_prompt: "Use $debug to find the root cause of this concrete failure before proposing a fix."

policy:
  allow_implicit_invocation: true
